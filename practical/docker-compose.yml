version: '3.8'

services:
  oracle-db:
    image: gvenzl/oracle-free:latest
    container_name: oracle-db
    ports:
      - "1521:1521"
    environment:
      - ORACLE_PASSWORD=oracle
    healthcheck:
      test: ["CMD-SHELL", "echo 'exit' | sqlplus -L sys/oracle@localhost:1521/FREE as sysdba || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  backend:
    build: .
    container_name: backend-1
    ports:
      - "8080:8080"
    depends_on:
      oracle-db:
        condition: service_healthy